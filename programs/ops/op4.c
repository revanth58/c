#include<stdio.h>

int main(){
	// understand the code, and try to run it and see the wierd behaviour
	// contact me for explanation

	char a,b;
	int x;
	scanf("%d",&x);
	scanf("%c",&a);
	scanf("%c",&b);
	printf("%d %c %c", x,a,b);
}